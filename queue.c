#include "algo.h"
typedef struct
{
    i64 *Head;
    i64 *Tail;
    u32 CurrentSize;
    u32 MaxSize;
} queue_t;

internal void
QueueInitialize(queue_t *Queue, void *Memory, u32 MaxSize)
{

}

// queue: add, remove, isfull, isempty
